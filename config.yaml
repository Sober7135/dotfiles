CompileFlags:
    Add:
      [
        # -std=c99,
        # -stdlib=libc++,
        # -std=c++2a
        -Wall,
        -Wextra,
        # -Werror,
      ]
    Compiler: clang
 
InlayHints:
  Enabled: Yes
  ParameterNames: Yes
  DeducedTypes: Yes
